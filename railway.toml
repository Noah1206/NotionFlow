[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "web"
buildCommand = ""
startCommand = "gunicorn frontend.app:app --bind 0.0.0.0:$PORT --workers 1 --log-level info"