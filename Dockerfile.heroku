# Alternative Dockerfile using Heroku's buildpack approach
# This avoids Docker Hub completely

FROM heroku/python:3.11

# Set working directory
WORKDIR /app

# Copy requirements
COPY requirements.txt .

# Install dependencies
RUN pip install --no-cache-dir -r requirements.txt

# Copy app
COPY . .

# Expose port
EXPOSE 8080

# Run command
CMD ["gunicorn", "frontend.app:app", "--bind", "0.0.0.0:$PORT", "--workers", "1"]